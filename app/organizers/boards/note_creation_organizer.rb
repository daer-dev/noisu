# frozen_string_literal: true

module Boards
  class NoteCreationOrganizer
    def run(*args)
      # Boards::NoteValidationInteractor.new(
      #   record_attrs: note_attrs
      # ).run
      #
      # Boards::NotePersistInteractor.new(
      #   board_slug:   board_slug,
      #   record_attrs: note_attrs
      # ).run
      #
      # Boards::NoteBroadcastInteractor.new(
      #   board_slug:   board_slug,
      #   note_id: note_id
      # ).run
      #
      # Boards::NotificationCreationOrganizer.new(
      #   board_slug:   board_slug,
      #   type: type,
      #   text: text,
      # ).run
    end
  end
end
